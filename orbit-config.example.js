/**
 * Orbit Configuration Example
 * 
 * Copy this file to orbit-config.js and fill in your Orbit credentials
 * OR set these as environment variables:
 * 
 * - ORBIT_BASE_URL
 * - ORBIT_API_KEY_NAME
 * - ORBIT_API_KEY_VAL
 * - ORBIT_PROJECT_ID
 */

module.exports = {
  baseUrl: 'http://localhost:80', // Your Orbit instance URL
  apiKeyName: 'YOUR_API_KEY_NAME', // Get from Orbit → Project → API Keys
  apiKeyVal: 'YOUR_API_KEY_VAL',   // Get from Orbit → Project → API Keys
  projectId: 1                     // Your Orbit project ID
};

